Missing features:

* Validate we don't overwrite an existing wiki with same name
* Consider not stripping all HTML.
* ID prefix?
* Documentation as a TW
* Tweaks to the publish format for Anki add-on

Possible glitches:
